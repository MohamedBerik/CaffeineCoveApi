{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "nixpacks": {
            "config": {
                "php": {
                    "extensions": ["redis"]
                }
            }
        }
    },
    "crons": {
        "schedule-run": {
            "schedule": "* * * * *",
            "command": "php artisan schedule:run"
        }
    }
}
